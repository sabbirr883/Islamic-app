import 'package:flutter/material.dart';

class MemorizesPage extends StatelessWidget {
  const MemorizesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('MemorizesPage'),),
    );
  }
}
