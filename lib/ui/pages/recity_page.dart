import 'package:flutter/material.dart';

class RecityPage extends StatelessWidget {
  const RecityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('RecityPage'),),
    );
  }
}
