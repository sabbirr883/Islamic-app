import 'package:flutter/material.dart';

class MemorizePage extends StatelessWidget {
  const MemorizePage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
appBar: AppBar(
  actions: [
    Image.asset("images/menu-2-line 1.svg")
  ],
),

    );
  }
}
