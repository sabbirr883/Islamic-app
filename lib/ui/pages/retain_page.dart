import 'package:flutter/material.dart';

class RetainPage extends StatelessWidget {
  const RetainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('RetainPage'),),
    );
  }
}
